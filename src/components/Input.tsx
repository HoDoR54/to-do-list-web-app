const Input = () => {
  return (
    <form action="submit">
      <input
        type="text"
        className="w-full px-5 py-2 bg-white rounded-md"
        placeholder="Add a new to-do"
      />
    </form>
  );
};

export default Input;
